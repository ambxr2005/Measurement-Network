# 🌐 Modular Internet Measurement Network

A hackathon-winning prototype for a modular internet measurement system using NATS.io.

## 🚀 Quick Start

```bash
# First build and then start
docker-compose build --no-cache
docker-compose up

# Start everything with one command
docker-compose up --build

# Or start in background
docker-compose up -d